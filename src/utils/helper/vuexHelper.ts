// import store from '/@/store';
// 先注释掉
// // 如果这个名字在store种注册过了，就注销这个名字
// export function hotModuleUnregisterModule(name: string) {
//   if (!name) return;
//   if ((store.state as any)[name]) {
//     store.unregisterModule(name);
//   }
// }

